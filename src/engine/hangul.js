/*! 내 계정 지킴이 — © 2026 티쳐무 · 모든 권리 보유
 *  두벌식 자판 ↔ 한글 바꾸기.
 *
 *  「사랑해」를 영문 상태로 치면 tkfkdgo 가 된다. 영어처럼 보여서 안전할 것 같지만,
 *  해커의 사전에는 이런 「한영 전환 낱말」도 이미 들어 있다. 이 파일은 그 바꾸기를 두 방향으로 한다.
 *    한글을자판으로('사랑해')  → 'tkfkdgo'
 *    자판을한글로('tkfkdgo')  → '사랑해'
 *
 *  한글 한 글자 = 첫소리(초성) + 가운뎃소리(중성) + 끝소리(종성, 없을 수도 있음).
 *  유니코드는 이 셋의 번호로 글자 번호를 계산한다: 0xAC00 + (초성×21 + 중성)×28 + 종성
 */

export const 초성 = ['ㄱ', 'ㄲ', 'ㄴ', 'ㄷ', 'ㄸ', 'ㄹ', 'ㅁ', 'ㅂ', 'ㅃ', 'ㅅ', 'ㅆ', 'ㅇ', 'ㅈ', 'ㅉ', 'ㅊ', 'ㅋ', 'ㅌ', 'ㅍ', 'ㅎ'];
export const 중성 = ['ㅏ', 'ㅐ', 'ㅑ', 'ㅒ', 'ㅓ', 'ㅔ', 'ㅕ', 'ㅖ', 'ㅗ', 'ㅘ', 'ㅙ', 'ㅚ', 'ㅛ', 'ㅜ', 'ㅝ', 'ㅞ', 'ㅟ', 'ㅠ', 'ㅡ', 'ㅢ', 'ㅣ'];
export const 종성 = ['', 'ㄱ', 'ㄲ', 'ㄳ', 'ㄴ', 'ㄵ', 'ㄶ', 'ㄷ', 'ㄹ', 'ㄺ', 'ㄻ', 'ㄼ', 'ㄽ', 'ㄾ', 'ㄿ', 'ㅀ', 'ㅁ', 'ㅂ', 'ㅄ', 'ㅅ', 'ㅆ', 'ㅇ', 'ㅈ', 'ㅊ', 'ㅋ', 'ㅌ', 'ㅍ', 'ㅎ'];

// 자모 하나 → 두벌식 글쇠. 쌍자음과 ㅒ·ㅖ 는 Shift 를 눌러 대문자가 된다.
const 자모글쇠 = {
  'ㄱ': 'r', 'ㄲ': 'R', 'ㄴ': 's', 'ㄷ': 'e', 'ㄸ': 'E', 'ㄹ': 'f', 'ㅁ': 'a', 'ㅂ': 'q', 'ㅃ': 'Q',
  'ㅅ': 't', 'ㅆ': 'T', 'ㅇ': 'd', 'ㅈ': 'w', 'ㅉ': 'W', 'ㅊ': 'c', 'ㅋ': 'z', 'ㅌ': 'x', 'ㅍ': 'v', 'ㅎ': 'g',
  'ㅏ': 'k', 'ㅐ': 'o', 'ㅑ': 'i', 'ㅒ': 'O', 'ㅓ': 'j', 'ㅔ': 'p', 'ㅕ': 'u', 'ㅖ': 'P', 'ㅗ': 'h',
  'ㅛ': 'y', 'ㅜ': 'n', 'ㅠ': 'b', 'ㅡ': 'm', 'ㅣ': 'l',
  // 겹모음·겹받침은 글쇠 두 개를 잇달아 누른다
  'ㅘ': 'hk', 'ㅙ': 'ho', 'ㅚ': 'hl', 'ㅝ': 'nj', 'ㅞ': 'np', 'ㅟ': 'nl', 'ㅢ': 'ml',
  'ㄳ': 'rt', 'ㄵ': 'sw', 'ㄶ': 'sg', 'ㄺ': 'fr', 'ㄻ': 'fa', 'ㄼ': 'fq', 'ㄽ': 'ft', 'ㄾ': 'fx',
  'ㄿ': 'fv', 'ㅀ': 'fg', 'ㅄ': 'qt',
};

// 글쇠 하나 → 자모 (한 글쇠짜리만)
const 글쇠자모 = {};
for (const [자모, 글쇠] of Object.entries(자모글쇠)) if (글쇠.length === 1) 글쇠자모[글쇠] = 자모;

const 모음들 = new Set(중성);
const 겹모음 = { 'ㅗㅏ': 'ㅘ', 'ㅗㅐ': 'ㅙ', 'ㅗㅣ': 'ㅚ', 'ㅜㅓ': 'ㅝ', 'ㅜㅔ': 'ㅞ', 'ㅜㅣ': 'ㅟ', 'ㅡㅣ': 'ㅢ' };
const 겹받침 = {
  'ㄱㅅ': 'ㄳ', 'ㄴㅈ': 'ㄵ', 'ㄴㅎ': 'ㄶ', 'ㄹㄱ': 'ㄺ', 'ㄹㅁ': 'ㄻ', 'ㄹㅂ': 'ㄼ',
  'ㄹㅅ': 'ㄽ', 'ㄹㅌ': 'ㄾ', 'ㄹㅍ': 'ㄿ', 'ㄹㅎ': 'ㅀ', 'ㅂㅅ': 'ㅄ',
};
const 겹받침나눔 = {};
for (const [둘, 하나] of Object.entries(겹받침)) 겹받침나눔[하나] = [둘[0], 둘[1]];

export function 한글음절인가(글자) {
  const c = String(글자).codePointAt(0);
  return c >= 0xac00 && c <= 0xd7a3;
}

// ── 한글 → 자판 ──────────────────────────────────────────────
// 글자 번호에서 초성·중성·종성 번호를 거꾸로 계산해 글쇠로 바꾼다.
export function 한글을자판으로(글) {
  let 결과 = '';
  for (const ch of String(글)) {
    if (한글음절인가(ch)) {
      const n = ch.codePointAt(0) - 0xac00;
      const 초 = Math.floor(n / 588);
      const 중 = Math.floor((n % 588) / 28);
      const 종 = n % 28;
      결과 += 자모글쇠[초성[초]] + 자모글쇠[중성[중]] + (종 ? 자모글쇠[종성[종]] : '');
    } else if (자모글쇠[ch]) {
      결과 += 자모글쇠[ch];
    } else {
      결과 += ch;
    }
  }
  return 결과;
}

// ── 자판 → 한글 ──────────────────────────────────────────────
// 글쇠를 하나씩 받아 글자를 조립한다(키보드의 한글 입력기가 하는 일과 같다).
//   자음 → 첫소리 / 모음 → 가운뎃소리 / 그 뒤 자음 → 끝소리
//   끝소리 뒤에 모음이 오면 끝소리가 다음 글자의 첫소리로 넘어간다 (예: 앉 + ㅏ → 안자)
function 글쇠를자모로(k) {
  if (글쇠자모[k]) return 글쇠자모[k];
  const 작은 = k.toLowerCase();
  // Shift 를 눌러도 쌍자음이 없는 글쇠는 같은 자모가 나온다 (Shift+a → ㅁ)
  if (k !== 작은 && 글쇠자모[작은]) return 글쇠자모[작은];
  return null;
}

function 글자짓기(초, 중, 종) {
  return String.fromCharCode(0xac00 + (초성.indexOf(초) * 21 + 중성.indexOf(중)) * 28 + (종 ? 종성.indexOf(종) : 0));
}

export function 자판을한글로(글) {
  let 결과 = '';
  let 초 = null;
  let 중 = null;
  let 종 = null;

  const 내보내기 = () => {
    if (초 && 중) 결과 += 글자짓기(초, 중, 종);
    else if (초) 결과 += 초;
    else if (중) 결과 += 중;
    초 = 중 = 종 = null;
  };

  for (const k of String(글)) {
    const j = 글쇠를자모로(k);
    if (!j) { 내보내기(); 결과 += k; continue; }

    if (모음들.has(j)) {
      if (초 && !중) { 중 = j; continue; }
      if (중 && !종) {
        const 겹 = 겹모음[중 + j];
        if (겹) { 중 = 겹; continue; }
        내보내기(); 중 = j; continue;
      }
      if (초 && 중 && 종) {
        // 끝소리가 다음 글자로 넘어간다. 겹받침이면 뒤쪽 하나만 넘어간다.
        let 넘길;
        if (겹받침나눔[종]) { [종, 넘길] = 겹받침나눔[종]; } else { 넘길 = 종; 종 = null; }
        내보내기(); 초 = 넘길; 중 = j; continue;
      }
      내보내기(); 중 = j; continue;
    }

    // 자음
    if (초 && 중) {
      if (!종) {
        if (종성.includes(j)) { 종 = j; continue; }  // ㄸ·ㅃ·ㅉ 은 끝소리가 될 수 없다
      } else {
        const 겹 = 겹받침[종 + j];
        if (겹) { 종 = 겹; continue; }
      }
    }
    내보내기(); 초 = j;
  }
  내보내기();
  return 결과;
}

// 비밀번호 안의 영문 글자를 한글 자판으로 쳤다고 보고 풀어 본다.
// 완성된 한글이 두 글자 이상 나오고, 풀린 글자 가운데 자모 조각이 적을 때만 「뜻 있는 한글」로 본다.
export function 한영풀이(글) {
  const 한글 = 자판을한글로(글);
  let 음절 = 0;
  let 조각 = 0;
  for (const ch of 한글) {
    if (한글음절인가(ch)) 음절++;
    else if (/[ㄱ-ㅣ]/.test(ch)) 조각++;
  }
  const 뜻있음 = 음절 >= 2 && 조각 <= Math.floor(음절 / 3);
  return { 한글, 음절, 조각, 뜻있음 };
}
