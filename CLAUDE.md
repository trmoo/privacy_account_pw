# privacy_account_pw — 🔐 내 계정 지킴이

© 2026 티쳐무 · 모든 권리 보유 (저작권 표시 일곱 곳 전부. 루트 CLAUDE.md 「저작권 표시」 방침)

## ① 목적과 대상 단원
- 고등학교 「정보」 Ⅴ. 디지털 문화 — **[12정05-02]** 올바른 정보 보호 방법 실천 · **[12정05-03]** 보안 기술 활용.
  성취기준 적용 시 고려 사항의 「개인 암호 설정 및 변경」, 「공급자 측면과 사용자 측면을 두루」를 겨냥했다
  (운영자 모형이 공급자 측면이다).
- **사용자 요청(2026-09-19)**: 블로그 두 편을 결합해 ① 회원가입한 사이트 조회하기 ② 내 비밀번호가 안전한지 테스트하기
  ③ 안전한 비밀번호 만드는 방법 알아보기 — 를 주제로 교육용 웹앱. 설계안 확인 때 **4탭 전부 · 유출 확인은 오프라인 전용**으로 결정.
  - 블로그 ①: m.blog.naver.com/sinnam88/224328085759 (개인정보 포털 웹사이트 회원탈퇴 조회·탈퇴 절차, 2026-06 캡처)
  - 블로그 ②: blog.naver.com/pentamkt/221757693554 (NIST 규칙 변천, KISA 기준, 피해야 할 비밀번호, 도메인 규칙법)
  - **문장·그림은 옮기지 않았다.** 주제와 흐름만 참고해 새로 썼고 사실은 공식 자료로 다시 확인했다.
  - 블로그의 틀린 곳(NIST 「2007년」 → 2004년 6월, 「한국인터넷산업진흥원」 → 한국인터넷진흥원, 2025년 7월 최신판 누락)은
    `src/data/content.js` 의 `바로잡은곳` 에 두고 「마무리 → 출처와 선생님께」에 보여 준다. **몰래 고치지 말 것.**

## ② 기능 (탭 4 · 화면 16 · 문항 63 · 활동 15)
| 탭 | 화면 |
|---|---|
| 🔎 `accounts` | why(도미노 실험: 같은 비번/다른 비번 × 메일 2단계) · portal(**연습용 포털 8단계** — 검색 결과에 가짜 광고 2개, 메뉴 4개 중 고르기, 조회 기간 표, 동의, 인증 수단별 주의, 5초 조회 중 「뒤로 가기」 함정, 가능/불가 탭, 접수·진행 확인 경로) · sort(정리 결정 게임 12곳) · more(메일함·소셜 로그인·저장된 비밀번호·문자함·털린 내 정보 찾기) |
| 🧪 `check` | tester(검사기 + 공격 4가지 시간표) · persona(가상 인물 도하람, 표적 공격) · battle(대결 5쌍 + 직접 대결) · math(길이 vs 종류 실험실 + 시간 격자) |
| 🛠️ `make` | history(연표 + 90일 변경 실험 + NIST 해야 할 것/하면 안 되는 것) · rules(해설서 기준 견주기·피할 것 5·NCSC 막대) · methods(도메인 규칙법 + **해커의 눈** / 문장 첫 글자 / 무작위 단어 잇기 / 무작위 생성기) · beyond(2단계 인증·패스키·피싱 표) · operator(**운영자 모형**: 규칙 9가지 × 회원 100명) |
| ✅ `finish` | quiz(종합 10) · pledge(점검표 8 + 다짐 + **활동지 .html 내려받기** + 기록 지우기) · about(출처·바로잡은 곳·개인정보 약속·수업 흐름) |

### 구조
- `src/engine/` — DOM 없음, node 로 시험한다.
  - `strength.js` **검사 엔진**: 조각 찾기(흔한·영어·이름·한영·한글·거꾸로·리트·**낱말잇기**·자판·연속·되풀이·날짜·연도·전화·개인정보·무작위)
    → 동적 계획법으로 최소 추측 → `(조각 수)! × Π추측 + 10000^(조각 수−1)` (zxcvbn 연구의 틀, 코드는 새로 씀). 모든 수는 log10.
  - `hangul.js` 두벌식 한영 변환(양방향) · `keyboard.js` 비스듬한 자판 이웃(94글쇠, 평균 이웃 4.x) · `lists.js` 흔한 비밀번호·한국어 비번 낱말·로마자 이름
  - `words-ko.js` 잇기 낱말 **1024개**(2^10, 서브에이전트 작성·시험으로 검사) · `words-en.js` 영어 낱말 600개
  - `makers.js` 도메인 규칙·규칙 거꾸로 찾기·문장 첫 글자·단어 잇기·무작위 생성 · `policy.js` 운영자 모형 · `random.js` 암호용/씨앗 무작위 · `format.js` 큰 수·시간 읽기
- `src/data/activities.js` **활동·문항의 단일 출처**. 파일 끝에서 보기 차례를 **정해진 규칙으로 돌린다**(정답이 첫 번째에 몰리지 않게).
  차례에 뜻이 있는 문항은 `고정: true`. 정리 게임(3보기)·OX·A/B 는 돌리지 않는다.
- `src/data/content.js` 글감 + **바깥 주소는 여기에만**(빌드 점검이 dist 속 주소를 이 목록과 대조).
- `src/lib/` ui(h·수명 관리·알림·비번칸) · store(기록) · quiz · report(활동지 HTML) · josa(은는·이가·을를·**와과**)

### ⚠ 지켜야 할 것
- **비밀번호는 어디에도 저장하지 않는다.** `check:syntax` ⑩ 이 `자료적기('…비번/비밀/문장/잇기…')` 와 store.js 밖의 localStorage 를 막는다.
- **통신 코드 금지** (`check:syntax` ⑨: fetch·XHR·WebSocket·sendBeacon·new Image). 배포본에는 빌드 때만 CSP meta 를 넣는다
  (`vite.config.js` 의 `보안정책넣기`, `connect-src 'none'`). 개발 서버에는 안 넣는다(HMR 이 막히므로).
- 학번·이름은 `finish.js` 모듈 변수에만 → 활동지 파일에만. 활동지에 학생 글은 반드시 이스케이프(시험이 `<script>` 로 확인).
- 연습용 포털·게임의 사이트 주소는 모두 `.example`. 가상 인물·사이트는 지어낸 것(시험이 지킨다).
- **대결 문항의 정답은 엔진 판정과 같아야 한다** — 엔진·낱말 목록을 고치면 `test/strength.test.mjs` 「비밀번호 대결」이 확인한다(10배 넘는 차이까지).
- 가상 인물 문항 1의 정답 보기만 개인정보가 없어야 한다(시험이 표적 모드로 확인).
- 가상 인물 조각은 **영문·숫자 3글자, 한글 2글자 이상만** — `7`·`77` 을 넣었더니 아무 비밀번호의 「77」이 등번호로 잡혔다
  (배포 전 검토에서 발견, 2026-09-20). 엔진(`strength.js` 개인정보 맞추기)도 같은 하한을 둔다.
- 경우의 수를 글로 적을 때는 시험으로 확인한다 — 해설에 「1,024⁵ ≈ 1.1경」이라 적었다가 실제는 **약 1,126조**였다(경=10¹⁶).
- 운영자 모형: 「새 지침이 옛 규칙보다 뚫림·불편·약한 비밀번호 모두 적다」, 「평문이면 100명 풀림」, 「2단계 필수면 0명」,
  「4종류 강요해도 쉬운 회원 25명 이상 약함」을 시험이 지킨다. 행동 규칙을 바꾸면 이 결론이 뒤집히지 않는지 볼 것.
  - 차단 목록은 NIST 대로 **통째 비교**다. 최소 15자 때문에 털린 비번 뒤에 숫자를 붙이면 통과하므로, 새 지침에서도
    「털린 비밀번호(와 그 변형)」 스터핑이 남는다 — 2단계 인증 필수에서만 0 이 된다. 일부러 그렇게 두었다.
- 탭 파일에서 setInterval·setTimeout·resize·requestAnimationFrame 직접 금지 → ui.js 의 화면타이머·나중에·창크기바뀔때·화면애니.
- 조사는 josa.js. 영문 사이트 이름 뒤 조사는 **읽는 소리**로 고른다(`make.js` 의 `읽는소리`: naver=네이버→와, daum=다음→과).
- 사실 수치(2026-09-19 확인): 포털 조회 기간 휴대폰 1년·신용카드 2년·주민번호/아이핀 5년, 하루 30건(2026-06 화면),
  털린 내 정보 찾기 kidc.eprivacy.go.kr(2021-11~), NIST SP 800-63B-4 2025년 7월, NCSC 2019-04(123456 약 2,320만),
  개인정보 보호법 제36조·시행령 제43조(10일). 공격 속도: 온라인 100/시간·10/초, 오프라인 느린 1만/초·빠른 1,000억/초
  (RTX 4090 MD5 약 1,640억/초 벤치마크를 어림). 등급은 「느린 암호화 1만/초」 기준 1분·하루·1년·100년.

### 점검
- `npm test` **622가지** · `npm run check:syntax`(소스 34개) · `npm run build` → `dist/index.html` 약 196KB · `npm run check:dist`
  (저작권 4곳 · CSP · 바깥 주소 10개가 모두 content.js 목록).
- 미리보기: `.claude/launch.json` 의 `privacy-dist`(5531, dist 를 python 으로) · `privacy-dev`(5532).
  브라우저 패널이 가려져 캡처가 안 되면 크롬 헤드리스로 찍는다(`--window-size=1366,2600 --screenshot=… http://localhost:5531/#make/operator`).
- 확인한 것(2026-09-19): 16화면 오류·「null/undefined/NaN」·남은 `**` 없음, CSP 아래에서 `fetch` 가 실제로 막힘,
  저장소에 입력한 비밀번호 흔적 없음, 휴대폰 너비(375px) 가로 넘침 없음, 포털 8단계·대결 공개·운영자 모형·다짐 동작.

## ③ 개발 상태
- 2026-09-19~20 첫 완성. 저장소: https://github.com/trmoo/privacy_account_pw (Pages: https://trmoo.github.io/privacy_account_pw/)
- 포털 `comedu_portal/` 「[디지털 문화] 학습 자료」 첫 카드로 올렸다.

### 다음 할 일(후보)
- 교사용 정답표·지도안, 학습지(.docx)
- 검사기에 「사이트 운영자가 쓰는 강도 기준」(zxcvbn 점수처럼) 거절 옵션을 운영자 모형에 더하기
- 피싱 사이트 알아보기(주소창 읽기) 실습 화면
